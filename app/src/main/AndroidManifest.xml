<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.baptistecarlier.android.samplefirebaseremoteconfiguration">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SampleFirebaseRemoteConfiguration">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:label="@string/nextlaunch"
            android:name=".ui.nextlaunch.NextLaunchActivity" />
        <activity
            android:label="@string/async" android:name=".ui.async.AsyncActivity" />
        <activity
            android:label="@string/sync" android:name=".ui.sync.SyncActivity" />
        <activity
            android:label="@string/result" android:name=".ui.result.ResultActivity" />
    </application>

</manifest>